\* Test stare path -- stare mode re-generates the ClientHello so
\* encrypted certs and session resumption do not apply.
\* EXPECTED: NoUnintendedSplice HOLDS -- stare mode is safe.

CONSTANTS
  Step1AclOutcome = 4
  Step2AclOutcome = 5
  Step3AclOutcome = 5
  ServerBehavior = "normalCert"
  HasValidationError = FALSE

SPECIFICATION Spec

INVARIANTS
  TypeInvariant
  DoneImpliesResolved
  NoUnintendedSplice
  BypassOnlyOnHardcodedPath

PROPERTY
  EventuallyResolves
