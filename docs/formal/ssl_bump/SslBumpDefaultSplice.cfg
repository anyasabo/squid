\* Test default-splice fallback when no ACL matches.
\* When BumpNone is returned at step 1 (admin has no ssl_bump rule,
\* or the rule returns "none"), the fallback must be splice/terminate.
\* EXPECTED: DefaultSpliceFallback HOLDS

CONSTANTS
  Step1AclOutcome = 0
  Step2AclOutcome = 0
  Step3AclOutcome = 0
  ServerBehavior = "normalCert"
  HasValidationError = FALSE

SPECIFICATION Spec

INVARIANTS
  TypeInvariant
  DoneImpliesResolved
  DefaultSpliceFallback

PROPERTY
  EventuallyResolves
