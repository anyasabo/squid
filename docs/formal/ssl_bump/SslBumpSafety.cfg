\* Safety-only config: check all ACL outcome x ServerBehavior combos.
\* This exhaustively explores whether admin-intended-bump can be
\* subverted to splice by server behavior.

CONSTANTS
  Step1AclOutcome = 3
  Step2AclOutcome = 3
  Step3AclOutcome = 5
  ServerBehavior = "encryptedCert"

SPECIFICATION Spec

INVARIANTS
  TypeInvariant
  DoneImpliesResolved
  NoUnintendedSplice
  BypassOnlyOnHardcodedPath
